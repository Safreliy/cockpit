FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

RUN pip install --no-cache-dir fastmcp "psycopg[binary]"

COPY tools ./tools

EXPOSE 8090

CMD ["python", "tools/cockpit_mcp_server.py", "--host", "0.0.0.0", "--port", "8090"]
